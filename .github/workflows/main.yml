name: Eslint / Prettier, checking formatting / common bugs
on:
  push:
    
  pull_request:
    branches: [main]

permissions:
  contents: write

jobs:
  format_check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Install modules
        run: yarn
      - name: Check if Prettier could format code
        run: npx prettier --check --no-config .